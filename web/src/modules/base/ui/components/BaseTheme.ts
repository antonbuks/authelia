import { createTheme } from "@mui/material";

import {
    breakpoints,
    colorMap,
    colors,
    font,
    overrides,
    shape,
    spacing,
    typography,
} from "@root/modules/base/ui/Tokens";

export default createTheme({
    font,
    colors,
    colorMap,
    spacing,
    shape,
    typography,
    breakpoints,
    overrides,
});
